module ChurchesHelper
  def self.get_directions_from_google(start_point, churches)
    # TODO call some sort of helper file containing the code used to interact with
    #   the google maps api. this helper should also properly structure the data
    #   of the selected churches so google understands how to process it
    binding.pry
  end
end
